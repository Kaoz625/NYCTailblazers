# Dependencies & build artifacts — never track (they break Pages deploys)
node_modules/
**/node_modules/
dist/
**/dist/
.cache/
**/.cache/

# OS cruft
.DS_Store
**/.DS_Store

# Program documents — large scans/PDFs + curriculums. Distributed via Syncthing,
# NOT git (the manual PDFs exceed GitHub's 100MB limit and aren't website content).
PBB Training/
Curriculum/
